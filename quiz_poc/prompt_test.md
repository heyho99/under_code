# Pythonコード読解・実装クイズ生成プロンプト（レベル1：基本文法・処理）

あなたはPythonのプログラミング教育の専門家です。
後述する【対象コード】を分析し、コード再現クイズを5問作成してください

## クイズ作成ルール
1.  **どのような内容か**: 
    *   対象コードに含まれる基本的な文法や処理を問うクイズ
    *   「関数定義やクラス定義を含まない、数行の処理」を対象とします
    *   コード内の特定の行やブロックを抜き出して問題にします
2.  **実行環境**: 
    *   ユーザーのコード実行時に、システム側から変数 `sysin` が与えられる前提です
    *   各テストケースごとに異なる値が `sysin` に代入されます
3.  **解答コードの形式**:
    *   与えられた `sysin` に対して処理を行い、結果を **標準出力（print）** するコードを正解とします
    *   必要なモジュールは `import` してください

## 出力フォーマット
以下のJSON形式に合わせて出力してください。

```JSON
{
    "1": {
        "title": "問題1のタイトル",
        "description": "問題文",
        "sysin_format": "sysin のデータ型や構造の説明",
        "test_cases": [
            {"sysin": "入力データ1", "expected": "期待される標準出力結果1"},
            {"sysin": "入力データ2", "expected": "期待される標準出力結果2"},
            {"sysin": "入力データ3", "expected": "期待される標準出力結果3"}
        ]
    },
    "2": {
        "title": "問題2のタイトル",
        "description": "問題文...",
        "sysin_format": "...",
        "test_cases": [
            {"sysin": "", "expected": ""},
            {"sysin": "", "expected": ""},
            {"sysin": "", "expected": ""}
        ]
    }
}
```

### 1. ルート要素
*   **キー (例: `1`, `2`)**:
    *   **説明**: 問題の通し番号（ID）
    *   **型**: 整数の文字列（"1","2"...）

### 2. 問題オブジェクト (`value`)
各問題IDに対応する詳細情報の定義

*   **`title`**:
    *   **説明**: 短く簡潔な問題の表題です
    *   **型**: 文字列 (String)

*   **`description`**:
    *   **説明**: ユーザー（解答者）に提示される問題文
    *   **必須要件**:
        1.  **入力**: 変数 `sysin` にデータが渡されることを明記してください
        2.  **処理**: 対象コードのロジックをどのように再現するか指示してください
    *   **型**: 文字列 (String)

*   **`sysin_format`**:
    *   **説明**: 変数 `sysin` にどのような形式のデータが入ってくるかの説明
    *   **型**: 文字列 (String)

*   **`test_cases`**:
    *   **説明**: 自動採点に使用されるテストデータのリスト。通常、3パターン程度用意（正常系、境界値、異常系など）
    *   **型**: リスト (List of Objects)

### 3. テストケースオブジェクト (内部要素)
`test_cases` リストの中身の定義

*   **`sysin`**:
    *   **説明**: 実行時にユーザーのコード内の変数 `sysin` に代入される具体的な値です
    *   **型**: 文字列、数値、リスト、辞書（JSONで表現可能な型）

*   **`expected`**:
    *   **説明**: ユーザーのコードが `print()` した結果と一致すべき**文字列**
    *   **型**: 文字列 (String) または 数値/真偽値（採点システムが文字列化して比較します）
    *   **注意**: 完全に一致（完全一致または改行空白削除後の完全一致）する必要があります


## 【対象コード】
```python
(ここにクイズの元となるソースコードを入力してください)
```

---



# Pythonコード読解・実装クイズ生成プロンプト（レベル2：関数実装）

あなたはPythonのプログラミング教育の専門家です。
後述する【対象コード】を分析し、そのコードに含まれる特定の関数のロジックを再現するクイズを2問作成してください。

## クイズ作成ルール
1.  **レベル定義**: レベル2は「1つのまとまった関数ロジックの再現」を対象とします。
    *   対象コード内の特定の関数（例: ステータスコード判定、値の検索など）と同じ振る舞いをする関数の実装を求めます。
2.  **実行環境**: 
    *   ユーザーのコード実行時に、システム側から変数 `sysin` が与えられる前提です。
3.  **解答コードの形式**:
    *   **関数を定義**する必要があります（関数名は任意で可、または指定）。
    *   定義した関数に `sysin` を引数として渡し、その戻り値を**標準出力（print）**するコードを正解とします。

## 出力フォーマット
以下のPython辞書形式に合わせて出力してください。

```python
{
    1: {
        "title": "問題1のタイトル",
        "description": "問題文。例: 対象コードの is_body_allowed_for_status_code 関数と同じロジックを持つ関数を定義し、整数 sysin を渡した結果（True/False）を print してください。",
        "sysin_format": "sysin のデータ型や構造の説明。例: 整数（ステータスコード）",
        "test_cases": [
            {"sysin": 200, "expected": "True"},
            {"sysin": 204, "expected": "False"},
            {"sysin": 404, "expected": "True"},
        ],
    },
    2: {
        "title": "問題2のタイトル",
        "description": "問題文...",
        "sysin_format": "...",
        "test_cases": [ ... ],
    },
}
```

## 【対象コード】
(ここにクイズの元となるソースコードを入力してください)


---


# Pythonコード読解・実装クイズ生成プロンプト（レベル3：モジュール・構造レベル）

あなたはPythonのプログラミング教育の専門家です。
後述する【対象コード】を分析し、コード全体の構造理解や、クラス・外部モジュール・複数関数の連携を問う応用クイズを2問作成してください。

## クイズ作成ルール
1.  **レベル定義**: レベル3は「クラスの利用、再帰処理、あるいはライブラリ固有の仕様を含む複合的な処理」を対象とします。
    *   例: クラスの初期化とメソッド呼び出し、キャッシュ機構の再現、警告（Warning）のハンドリングなど。
2.  **実行環境**: 
    *   ユーザーのコード実行時に、システム側から変数 `sysin` が与えられる前提です。
3.  **解答コードの形式**:
    *   クラス定義や複数の関数定義、あるいは特定のモジュール（`weakref`, `warnings`など）のインポートを含みます。
    *   `sysin` のデータを用いて構築したオブジェクトや処理の結果を操作し、最終的な値を**標準出力（print）**するコードを正解とします。

## 出力フォーマット
以下のPython辞書形式に合わせて出力してください。

```python
{
    1: {
        "title": "問題1のタイトル",
        "description": "問題文。例: 対象コード同様に WeakKeyDictionary を用いたキャッシュ変数を初期化し、sysin で与えられたキーと値を保存した後、そのキャッシュオブジェクト自体を print してください。",
        "sysin_format": "sysin のデータ型や構造の説明。例: 辞書 {'key': '...', 'value': '...'}",
        "test_cases": [
            {"sysin": {"k": "A", "v": 1}, "expected": "期待される出力"},
            # test_casesはsysinの構造に合わせて適切に設定してください
        ],
    },
    2: {
        "title": "問題2のタイトル",
        "description": "問題文...",
        "sysin_format": "...",
        "test_cases": [ ... ],
    },
}
```

## 【対象コード】
(ここにクイズの元となるソースコードを入力してください)
