# quiz_sets 一覧
docker compose exec quiz-db \
  psql -U user -d quiz_db \
  -c "SELECT id, user_id, title, description, created_at FROM quiz_sets ORDER BY id;"

# problems 一覧
# シングルクォート版（こっちの方がエスケープ少なくて楽）
docker compose exec quiz-db \
  psql -U user -d quiz_db \
  -c 'SELECT id, description, content_markdown, sample_answer FROM problems WHERE id = 1' \
  > output.txt

# generator-service に投げて JSON を取得:
curl -X POST http://localhost:8085/api/v1/generate \
  -H "Content-Type: application/json" \
  -d @generator_payload.json

